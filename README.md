# JunzheShao98.github.io

Create a GitHub repository named `JunzheShao98.github.io` and publish these files with GitHub Pages.

This copy is configured to publish the full homepage at:

- `https://junzheshao98.github.io/`

Key difference from the `homepage` project-site repo:

- `_config.yml` uses `baseurl: ""` so the site builds at the root URL.
